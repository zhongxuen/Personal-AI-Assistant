fastapi==0.115.6
python-multipart==0.0.20
uvicorn[standard]==0.34.0
pydantic==2.13.4
pydantic-settings==2.7.1
sqlalchemy==2.0.36
python-dotenv==1.0.1
psutil==6.1.1
dateparser==1.4.2
APScheduler==3.11.3
winotify==1.1.0
google-genai==2.20.0
faster-whisper==1.1.0
pyttsx3==2.98
pyperclip==1.9.0
PyJWT==2.10.1
discord.py==2.4.0
pywebpush==2.4.0
httpx==0.28.1
# Ships the IANA timezone database for `zoneinfo` (app/core/clock.py). Pulled in
# transitively today, pinned explicitly because the app now depends on it directly --
# without it, ZoneInfo("Asia/Kuala_Lumpur") raises on any host with no system tzdata
# (notably Windows).
tzdata==2026.3
